// import { lazy, Suspense } from "react";

// function load(importPath, props) {
//   const Path = lazy(() => importPath);

//   return (
//     <Suspense fallback={null}>
//       <Path {...props} />
//     </Suspense>
//   );
// }

// export const Bar = (props) => load(import("./bar"), props);

import { Dimensions } from "react-native";

export const { width: vw, height: vh } = Dimensions.get("screen");

export { Platform } from "react-native";

export { defineProperties } from "@pascinso/utils";
